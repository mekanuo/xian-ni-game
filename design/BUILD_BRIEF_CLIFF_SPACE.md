# 结果覆盖：当前布局已搁置

空间模型r1/r2已实际测试；r1源头可走，r2一次局部修订后默认寻路进入内侧。两次均4合同通过/1失败，r2类型检查通过。证据和可重放源码合同在qa/whitebox/cliff-space；活动原型源码/测试已移至快照，未留下伪装为通过的失败套件。没有浏览器、落石、完整章节或生产美术。root选择搁置当前布局，不再扩大本候选；不是对全部自然危险玩法的否决。

下面保留当时的构建假设。

# 碎崖空间白盒：第一闸口

2026-09-11。root在既定自主开发授权内决定开始这一有界检查。buildStage=whitebox，buildPath=custom，目标网页Phaser/TypeScript，testedRuntime=NOT_RUN；不是正式第八图或完整碎崖章节批准。

基于0.7最终说明2575722，独立工作树cliff-080-space。新分支依赖与基线35文件753测试通过；正式749测试对应冻结发行时尚未生成的两份完成档，新增夹具产生4个额外基线测试。

最大问题：两段危险是否要求不同判断，并能在手机普通景别下清楚行动。当前只验证前提空间，完整零灵力危险往返仍未构建。

## 候选1的完整几何生成方式

采用草案坐标作为待推翻输入。用清楚列出的可走矩形/通路带与两处狭窄出石缝定义地面，外围按10单位单元生成合并矩形障碍；实际行走仍使用原引擎的17轴向包络、40网格、160步速。岩鼻显式实体优先，不被通路带覆盖。生成法及全部参数进入只读几何证据，不能只写“周围不通”。狭出石缝是物理窄缝，射弹可过、身体不可过，不伪装成水面特殊碰撞。

第一段入口→A→B。安全连接候选由B向东到(1260,850)，向下到(1260,1820)，再到(890,1820)，从L下方接入；连接宽100，远离H/U/E，只可通过L进入第二段。两路仍从草案L、O、T、H、U、E取值。这些新增连接坐标是待测几何，没有通过结论。

## 最先的反例与证据

1. 真实move/tick往返，4体力/0灵力/0药；尚无落石，所以这里只证明地形可走。
2. H合法角点到U真实到达，不能中心插值或缩小身体。
3. 从B实际点E，记录是否先到L；另用测试内切断唯一连接的反事实检查查捷径，运行地图不增加隐藏墙。
4. 从L和合法边缘直接点E，记录实际路径而非脚本替玩家选H。若寻路自动完成整个遮挡选择，原风险仍未解决。
5. 源头及背后不能通过宽通路绕行。尤其S2可能落在草案上口宽带内，须让测试暴露问题，再局部修订。
6. 实际射弹源口、首次岩鼻接触与24受击圆另一步验证；不能拿free(17)当伤害安全判据。

先保留RED及具体路径，再修最小几何。路径成立后才做三视口最小页面，用明确普通地区镜头匹配HUD；不直接复用home桌面特例，不生成正式美术。若空间修改后仍无选择价值，可搁置。没有自然时长、趣味或整个小说完成承诺。

来源：沿现有SOURCE_SCOPE与碎崖风险审查；该地点、采石人、几何和危险均open原创，不替代王林经历。此白盒不接正式七图、正式存档版本和公网发行。
