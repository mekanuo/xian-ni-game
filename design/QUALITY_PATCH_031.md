# 0.3.1：动作与近处信息精修

2026-09-11。用户要求按上一轮顺序继续开发，先完成现有体验精修，再收敛0.4新冒险。本次为已获授权的现有表现修订，production/custom，沿用Phaser浏览器运行时与原地图、规则、存档版本。

- 采叶按实际目标朝向、真实剪取进度显示轻微俯身，试压按实际一秒工序前倾；施术、牵引、受击按实际状态区分姿态。
- 行走仅移动人物图像，阴影留在实际脚点；读档、换图重置视觉步态，不修改人物位置或战斗朝向。
- 暂停、对话、失焦时反馈与人物动作冻结；减少动态设置保持静态人物和必要的工序标识。
- 近处名称重叠时优先保留指向或最近物件的名称，不漂移文字锚点；路牌标签保持牌面位置，隐藏名称不影响实际点击与近身按钮。
- 修复手机暂停提示与察看按钮重叠：折叠时并排，展开察看时隐藏重复的中央暂停提示，底部继续按钮保留。

验证：纯表现函数回归、实际旧完成档的手机/桌面采叶动作与暂停、减少动态、密集工位标签布局；完整发布使用npm run verify并核对公网。人物仍使用既有单姿态原画加程序动作，不宣称已有完整逐帧动画。新区域0.4仅设计候选，见ADVENTURE_040.md。

## 发行补充：载入保持暂停

在续章审阅中复现：运行时打开设置再换档，旧菜单关闭逻辑可能短暂恢复新档，提前执行pending动作或消耗留势。导入/手动载入现在先明确新世界暂停，再关闭旧菜单；完成首章的存档不重弹首章结局。新增10项实际UI方法回归，不改存档格式、资源或关卡规则。

基础精修全矩阵已PASS（147模型/表现测试、两条首章新建至结局重开、生活两法、桌面/触屏与地区美术），保存qa/verification-baseline-031.json。后续UI修复157项测试和构建通过；对最终包补查UI存档、手机操作与暂停表现，结果并入qa/verification.json，不把先前包的完整流程冒充最终包重跑。
