# 划线切磋：模型独立审查

2026-09-11。范围：`SPAR_080_DESIGN.md`、`src/whitebox/spar-model.ts`、其专项测试，以及共享模型的移动／敌人／射弹顺序；顺带静态核对正在接入的 `spar-main.ts`。只审查本次独立一手灰盒，不作第八章生产批准。

方法：只读源码与临时 Node TypeScript 内存加载，调用真实 `createSpar / sparAct / sparTick`；没有改玩家、敌人、资源或回合状态来制造以下两项反例。没有执行浏览器、构建、Git 或完整测试。行号对应首次取证版本，根代理正在修复，不能把本报告当作修复后的 PASS。

## 1. 必须修复：越线同一小步仍能新发一弹

位置：`spar-model.ts:47–55` 的前后越线检查；共享 `model.ts:679–702` 在玩家物理移动之后才调用 `enemyTick`，后者 `model.ts:547` 达到两秒立即发射。

真实输入复现（普通合法零灵力初态）：

1. `agree`；点击 `(900,940)`，以 `.025` 秒小步运行 4 秒，闻朔实际走到对面；`begin`。
2. 运行 67 个小步（1.675 秒），点击 `(1020,940)`。
3. 再运行 13 个小步，实际玩家 `(952,940)`，距圆心 52，`attack=1.999999999999997`、`shots=0`。
4. 下一小步玩家先移到 `(956,940)`，距圆心 56，已不满足身体中心 ≤53；同小步却产生 `projectileSeq=1`，随后 wrapper 才进入 `settling / outside`。

取证时新弹位置 `(907.5237,749.5575)`、剩余寿命 1.575 秒，说明确实发生了发射与飞行，绝非渲染残影。这个新弹不是玩家越线之前已经在途的余弹。当前常规“开局就直接走出线”测试绕过了发射临界帧，不能挡住此反例。

最小修复方向：在真实玩家移动完成、敌人起手／发射之前提供窄的回调检查，由灰盒以实际身体位置收手。不要在 wrapper 复制导航、预测移动或清掉刚生成的弹来冒充先前没有发射。根代理已接管此修复和真实边界回归；本审查未验收其修改。

## 2. 必须修复：收手按钮覆盖已经成立的越线原因

位置：`spar-model.ts:22–23,39,58`。`stop` 无条件赋值原因，且允许 `settling` 再次进入。

接上例，在 `phase=settling / stopReason=outside` 且余弹尚在时，实际调用一次 `stop`，继续运行到弹结清，结果成为 `outcome=stopped`，原本已经成立的越线事实消失。取证最终 HP4、shots1、phase=result；没有注入伤害或改结果。

最小修复方向：保留第一个停止原因；重复收手可幂等接受或明确已收手，不能改写原因。之后的真实余弹仍应造成伤害，HP 与伤害反馈独立保留。需要一条“越线后余弹未清，再按收手”的回归；根代理已接管。

## 3. 目前没有确认漏洞的边界

- **约定与就位分开。** 初态近身约定只转 `positioning`；闻朔由真实 `moveNpc` 每步前行；`begin` 同时检查玩家全身在线内、闻朔离站位 ≤3、起手清洁条件。未看到瞬移就位、等够时间直接判胜的分支。
- **重复练习基线已补。** 最新 `agree` 同时重设 `startHp / startSeq`。临时真实回放先站定受一发（HP3、seq1），实际走近闻朔再约，然后在尚未 begin 时收手，最终 `shots=0 / outcome=stopped`，没有把上一回的射弹算成本回。此项以实际读取的新版本为准，不报告旧版本疑点。
- **单次伤害界限有明确前提。** 本初态仅一个 raider、无环境伤害；新约定和 begin 要求 HP≥2、无在途弹／玩家待发火球／牵物，入口只允许步法和护符。wrapper 观测到第一次发射后，下一小步在共享敌人 tick 前置 peaceful；共享射击间隔 2 秒远大于 `.025` 小步，因此不会在两个观测点之间发两次。单发寿命 1.6 秒、普通伤害一格。此处可推导单回合 HP 至少剩 1，但不等于没有受伤，也不适用于未来加入火球或第二攻击源。
- **余弹没有被和平状态删除。** 主动收手只停止后续敌人行动，仍执行实际 `projectileTick`，结清之前不能再约。现有“收手后仍受一格伤”测试与该顺序一致。玩家主动火球已在 wrapper 拒绝，暂停待办也不能绕入；因此本灰盒没有玩家第二枚火球跨回合的问题，未来扩展进攻时仍须另审。
- **暂停不暗中推进。** `sparTick` 对 paused／dialogue 直接返回；暂停的 move／ward 通过原 `act` 排队，agree／begin／stop 被拒绝。没有通过暂停改回合的正常动作路径。恢复后仍要按真实移动判越线，不能只检查恢复前脚点。
- **站定不构成胜利。** 结果需要实际 shot，且无伤／未越线才能给 dodged 或 blocked。当前无障碍、正常距离内站定实际受伤；既有零灵力点击 `(948,940)` 是在真实弹已出手之后移动，不能外推成任意慢输入都能避开。

## 4. 显示与实机闸口仍须完成

`model.ts` 原 warning 文本仍提“火球命中可以打断”，但当前 `spar-main.ts:63` 已把该事件显示为本回合的看方向提示，没有发现将被禁用火球直接教给玩家的屏幕分支。不要未来又把原 event.text 不加选择地输出到此灰盒。

当前静态镜头是独立局部 600×560 框，未继承整张 home 缩放。仍未实际证明：390 手机按键不遮圆线、护符提前张开导致闻朔真实侧移时身体与抬手全部可见、零灵力从看见发射到单指点击有正常反应余量。位于圆线边缘的角色画面脚形与中心53判据也需目视，不能以测试坐标正确替代。

建议根代理先完成上述两条时序回归，再执行有界真实输入；保留这份修复前反例，不把当前独立审查写成浏览器通过或主观手感认可。本报告交回后停止写入。
