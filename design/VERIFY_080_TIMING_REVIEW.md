# 0.8 剩余旧章：模拟时间与墙钟上限的定点复核

2026-09-12，只读审查。R5旧窑自然烧尽在60墙钟秒内只推进约3.767模拟秒，是已留档的实际失败；以下三项仅为静态同类风险，尚未在本轮复现，不记FAIL，也不预填专项PASS。未运行浏览器、测试或构建；未改脚本与游戏。

审查入口为 `scripts/verify.mjs:47` 的生活、留势、手机生活、旧渠与重访序列。生活脚本默认状态等待15秒，手机生活12秒，旧渠及重访15秒。R5比例约15.93墙钟秒/模拟秒仅用于识别脆弱上限，不能假定每个场景同样慢。

## 三个明确风险

| 位置与等待对象 | 真实规则与为何脆弱 | 最窄处理建议 |
|---|---|---|
| `scripts/life-check.mjs:147,151` 等叶片入袋；`scripts/life-mobile-check.mjs:55–56` 重试采叶 | `src/game/life.ts:145` 必须连续静止采集2模拟秒。桌面只有15墙钟秒；手机先固定等2.3墙钟秒，再以12秒等入袋，总约14.3秒。按R5比例，完整2模拟秒约需31.9墙钟秒，且固定2.3秒不能当作采集已足够推进。 | 仅把这三个“入袋完成”的自然等待显式放宽至90秒。保留采集中、移动中断、恢复未摘状态、暂停冻结和资源判据；无需扩大tap响应/开始采集等待，也无需延长850毫秒冻结观察。若采集已被中断或人物受击，不能靠长等待称其恢复。 |
| `scripts/canal-check.mjs:123,127,130,148,151` 等分水/退水/清筛/复位完成 | `src/game/canal.ts:161,167–172` 分水操作2模拟秒后才改变板位，随后另需1秒退水；清筛2秒；复位2秒；手机留势后的退水最多另需1秒。各调用继承15墙钟秒。分水加退水约3模拟秒，在R5比例下约47.8墙钟秒；单独2秒工作也超过15秒。 | 对分水且退水到底这一复合自然结果可单独给120秒，对清筛/复位各90秒，手机仅退水完成给60秒。手机留势全段继续真实运行，**不得增加暂停、延长留势或降低返高岸至少1.5秒余量**。动作被中断、涌水或已失去支撑仍是规则失败，不能为了等到cleared反复重试。 |
| `scripts/canal-revisit-check.mjs:98` 复位后等 `canal.flow===3`；同文件:95 分水完成 | 复位首先实际工作2模拟秒；恢复流水后 `src/game/canal.ts:162` 再累计3模拟秒才到flow3。一个15墙钟秒等待承载约5模拟秒，比单次工作更易先到截止；按R5比例约79.6墙钟秒。:95同样等2秒分水工作完成。 | 优先单独验证这条重访；flow3这个复合等待可显式给150秒，:95分水给90秒。保留板位、工作结束、完成章节状态、任务提示复位→桌边的原判据，不把flow达到较小值算通过。 |

## 不应一并放宽的项目

暂停期间的850/1100毫秒观察是在验证冻结，UI导入/对白出现是在验证输入结果，均不是“还需数秒模拟时长”的完成等待。镜头pan后的短稳定等待、按键140毫秒、点击定位与长路行走也有各自问题，不能借本次燃烧证据统一调大。

留势生活 `scripts/life-hold-check.mjs:130` 的试压由 `life.ts:143` 累计1模拟秒完成，15秒上限在极慢场景也可能临界；其火球软胶则等待命中，不是固定燃烧12秒。两者不列为本次前三项优先修改对象。该脚本1100毫秒暂停观察仍保持原值。未运行到它们，不能称已排除风险。

下一冻结前建议先观察“重访复位累计5秒”，再核旧渠两条自然工作路线与手机采叶。若仍沿用整轮顺序，至少在失败证据中保留真实time、工作elapsed、paused/dialogue和物件状态，区分自然过程未跑完与输入未成立。加长截止只给正常模拟推进时间；不允许注入时钟、提高游戏速度、手写完成状态或借旧PASS顶替本轮。
