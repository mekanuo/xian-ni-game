# 碎崖空间先验：搁置当前布局

基于2575722，独立临时地图替换home槽，只通过原有act/tick真实走路。初态明确合成；没有落石、浏览器或正式美术。

- r1：5合同中4通过，源头(930,1380)身体可站导致1失败。
- r2：一次局部修订后源头不可走、两路/边角/连接可达；直接点远端却自动进入内侧，合同仍4通过1失败。最大x1039.979，119路径样本包含初态。tsc exit0。

root决定搁置这版空间，不扩大制作去强留“碎崖下一章”。自动进入内侧是已测事实；由此判断主动掩蔽选择价值不足是设计取舍，不能宣称未实现的落石被无伤通过，也不否决所有自然危险题材。

源码、合同、失败日志和SHA均逐版本原样保存。活动src/tests已移回发行基线，原失败没有改为通过或跳过。要重现，先在2575722独立工作树安装依赖，把对应source快照复制到src/whitebox/cliff-space.ts、contract快照复制到tests/cliff-space.test.ts，再运行npx vitest run tests/cliff-space.test.ts；预期exit1及对应失败。只在隔离工作树重放，勿往发布候选注入失败原型。

此目录不进入试玩包；0.7公网保持原发行。下一内容按新的局部概念评审选择，不把地图数当完成标准。
